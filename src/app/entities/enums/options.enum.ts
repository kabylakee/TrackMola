export enum OptionsTitle {
	Copy = 'Copy to',
	Remove = 'Remove',
	Move = 'Move to',
}

export enum OptionsIcon {
	Copy = 'content_copy',
	Remove = 'delete',
	Move = 'autorenew',
}

export enum OptionsEvent {
	Copy = 'copyTo',
	Remove = 'remove',
	Move = 'moveTo',
}
